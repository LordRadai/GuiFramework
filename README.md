# GuiFramework
Dark Souls II debug menu framework. Links at runtime to the game itself, only meant to be used in a DLL mod. Won't be usable separately.

### Usage
Add this repository as a submodule, then add the GS project to your solution. You can then include GuiFramework.h and use the library normally.
